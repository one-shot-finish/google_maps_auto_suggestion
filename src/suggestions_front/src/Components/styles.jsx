import styled from 'styled-components';

export const Div = styled.div`
	position: absolute;
	top: 0;
	left: 0;
`;

export const MapDiv = styled.div`
	position: absolute;
	margin-top: 20px;
	bottom: 0;
`;